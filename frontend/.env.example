VITE_API_URL=http://localhost:3000/api

# Quando "true", o app inicializa o Mock Service Worker no browser e serve
# os dados mockados em src/mocks em vez de bater no backend real. Util para
# rodar o frontend isoladamente, sem o backend/Docker no ar.
VITE_ENABLE_MOCKS=false
